<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= image_path Decision.first.photo %>')">
    <div class="banner-countdown">
        <h3>
          <i class="far fa-calendar-alt"></i>
          J-<%= (Decision.first.council_date - Date.today).to_i%> avant le prochain conseil municipal

        </h3>
    </div>
    <div class="banner-content">
      <div class="banner-buttons">
        <h2><%= link_to Decision.first.category, decisions_path, class: "btn-category" %>
        </h2>
        <h2><%= link_to "Next Decisions", decisions_path, class: "btn-category-decisions" %></h2>
      </div>
      <%= link_to decisions_path do %>
        <div class="white-background">
          <h3><%= Decision.last.title %></h3>
          <p><%= Decision.last.description %></p>
        </div>
      <% end %>
      <div class="call_to_action text-center">

      </div>
    </div>

</div>
